# Local development: point to your Ollama instance.
# When this is set, the app calls Ollama directly from the browser (no backend needed).
# Leave unset in production — the Netlify Function will use the Anthropic SDK instead.
VITE_OLLAMA_URL=http://localhost:11434/api/chat
VITE_OLLAMA_MODEL=qwen2.5-coder
